spring:
  datasource:
    url: jdbc:h2:mem:nido;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

nido:
  security:
    jwt-secret: nido-test-secret-key-for-jwt-signing-2026
    token-minutes: 30
  demo-data: false
